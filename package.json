{
  "name": "azure-to-do-list",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose up --build",
    "start:azure-functions": "func start"
  },
  "dependencies": {
    "@azure/cosmos": "3.15.1",
    "dotenv": "^16.0.0"
  }
}
